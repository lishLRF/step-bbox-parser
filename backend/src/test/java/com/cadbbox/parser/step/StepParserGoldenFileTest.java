package com.cadbbox.parser.step;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Placeholder golden-file test. The real test will load each file in
 * {@code samples/step/}, run it through the parser and assert the resulting
 * tree + bbox matches {@code samples/bbox-output/}.
 */
class StepParserGoldenFileTest {

    @Test
    void scaffolding_is_in_place() {
        assertThat(true).isTrue();
    }
}
