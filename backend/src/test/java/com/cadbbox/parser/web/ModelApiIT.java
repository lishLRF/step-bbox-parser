package com.cadbbox.parser.web;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Placeholder for the REST API integration test (Spring Boot @SpringBootTest
 * with MockMvc). Once {@code ModelController} exists, this will exercise the
 * upload → tree → bbox flow end to end.
 */
class ModelApiIT {

    @Test
    void scaffolding_is_in_place() {
        assertThat(true).isTrue();
    }
}
